numpy>=1.24
pygame-ce>=2.5 ; python_version >= "3.13"   # drop-in fork of pygame (`import pygame`)
pygame>=2.5 ; python_version < "3.13"
websockets>=12.0
pymavlink>=2.4   # MAVLink GCS bridge (--mavlink); interface degrades gracefully without it
